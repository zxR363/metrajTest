# Tip kodu sablonu. Her firma kendi tip standardini buraya yazar.
#
# Onerilen kodlama:
#   D1, D2, D3 ... = Doseme tipleri
#   W1, W2, W3 ... = Duvar tipleri
#   T1, T2, T3 ... = Tavan tipleri
#   S1, S2, S3 ... = Supurgelik tipleri
#
# pay alani 0..1 arasi yuzdedir; 1 mahalde birden cok kaplama
# kullaniliyorsa toplam 1.0'dan az olabilir.
tipler:
  # === DOSEME ===
  D1:
    kategori: DOSEME
    tanim: "Genel kullanim - 60x60 porselen karo"
    pozlar:
      - {poz_no: "15.385.1028", pay: 1.0}
      - {poz_no: "15.250.1011", pay: 1.0}
      - {poz_no: "15.250.1111", pay: 1.0}

  # === DUVAR ===
  W1:
    kategori: DUVAR
    tanim: "Kaba siva + saten + boya"
    pozlar:
      - {poz_no: "15.275.1116", pay: 1.0}
      - {poz_no: "15.280.1011", pay: 1.0}
      - {poz_no: "15.540.1531", pay: 1.0}

  # === TAVAN ===
  T1:
    kategori: TAVAN
    tanim: "Sivali tavan + boya"
    pozlar:
      - {poz_no: "15.275.1113", pay: 1.0}
      - {poz_no: "15.540.1523", pay: 1.0}

  # === SUPURGELIK ===
  S1:
    kategori: SUPURGELIK
    tanim: "Terrazo supurgelik"
    pozlar:
      - {poz_no: "15.405.1701", pay: 1.0}
